interface:
  display_name: "Pet Studio"
  short_description: "Build style-matched Codex pet rooms, helpers, and bubble-connected widgets."
  default_prompt: "Use $pet-studio to create or maintain a style-matched Codex pet room."
policy:
  allow_implicit_invocation: true
