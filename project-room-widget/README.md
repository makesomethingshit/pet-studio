# Legacy Project Room Widget Path

This folder is kept only as a compatibility shim. The active Pet Studio widget runtime lives in:

```text
pet-studio-widget/
```

Use `pet-studio-widget/pet_studio_widget.py` for new work.
